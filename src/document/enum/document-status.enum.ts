export enum DOCUMENT_STATUS_ENUM {
    PENDING = 'pending',
    SIGNED = 'signed',
    REJECTED = 'rejected',
    EXPIRED = 'expired',
    CREATED = 'created',
    CANCELLATION_PENDING = 'cancellation_pending',
    CANCELLED = 'cancelled',
}
