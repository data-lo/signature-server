/** Idéntico a DOCUMENT_PARTICIPANT_STATUS_ENUM — renombrado a SIGNEE_STATUS para coincidir con el diagrama ER-V2. */
export enum SIGNEE_STATUS_ENUM {
  PENDING = 'pending',
  SIGNED = 'signed',
  REJECTED = 'rejected',
}
