/** SIMPLE ya está implementado (imagen PNG). FIEL es modelo de datos únicamente (ver plan de migración ER-V2, Fase 8) — sin lógica de firma criptográfica todavía. */
export enum SIGNATURE_TYPE_ENUM {
  SIMPLE = 'simple',
  FIEL = 'fiel',
}
