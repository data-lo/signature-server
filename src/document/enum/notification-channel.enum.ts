/** SMS por PHONE_NUMBER queda fuera de alcance hoy (sin proveedor integrado) — solo EMAIL tiene un caller real. */
export enum NOTIFICATION_CHANNEL_ENUM {
  EMAIL = 'email',
  PHONE_NUMBER = 'phone_number',
}
