export enum RESOURCE_KEY_ENUM {
  DOCUMENT = 'DOCUMENT',
  ORGANIZATION = 'ORGANIZATION',
  USER = 'USER',
  /** Miembros de una organización; lo estrena el catálogo estático (MEMBER.INVITE). */
  MEMBER = 'MEMBER',
}
